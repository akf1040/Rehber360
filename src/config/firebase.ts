import auth from '@react-native-firebase/auth';
import firestore from '@react-native-firebase/firestore';

// Firebase yapılandırması otomatik olarak google-services.json'dan alınıyor
// React Native Firebase için ayrıca yapılandırma yapmaya gerek yok

export { auth, firestore }; 